git
__________________________
git clone link
git add .
git commit -m"djfkj"
git push


basic command
___________________________
ls-list
cd-change directory
code .-open path
len()-length of a string
num%2-get the reminder
and-&&

function
__________________________
parameters-items that we passing to a function
type-(string,int,array....)
func definition,call
function can return outputs
    function can return single or multiple outputs
    for returning outputs we must define their types(int,string)
    output can also be struct
struct
__________________________
how to define a struct-type <struct name> struct
should mention required properties
to take the value from struct-(.)

array and slice
__________________________
array
    fixed length
    show size and type->[4]int{1,2,3,4,5}
    We can also add elements to an array by concatenation
    eg:b1 := "jackson"
	b1 = b1 + " "
	b1 = b1 + "james"
	fmt.Println(b1)

slice->
    not showing length->[]int{2,3,4,5}
    add the length by append()keyword


to find the len->len()
unsing for loops by initializing a variable,giving condition,and increment or decrement
eg-> for 1:=0;1<4;1++

star printing
__________________________
for i := 1; i <= 5; i++ { //i=3,true
		printStar(i)
	}
func printStar(count int) {
	star := "*"
	for i := 1; i < count; i++ {
		star = star + "*"
	}
	fmt.Println(star)
}

